# Metadata section

name: aramanau-rock-phase2
summary: Hello World for phase2
description: The basic example of a rock for SD phase2.
version: "latest"
license: Apache-2.0

base: bare
build-base: ubuntu@22.04
platforms:
  amd64:  # Make sure this value matches your computer's architecture

# Parts section

parts:
  hello:
    plugin: nil
    stage-packages:
      - hello
